---
# This playbook runs entirely locally against the PaletteAI/Dex APIs and
# the EKS cluster via kubectl. Only localhost is needed as a target.
all:
  hosts:
    localhost:
      ansible_connection: local
      ansible_python_interpreter: "{{ ansible_playbook_python }}"
